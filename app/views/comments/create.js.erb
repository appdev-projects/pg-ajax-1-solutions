var comment = $("<%= j(render @comment) %>");

comment.hide();

$("#<%= dom_id(@comment.photo) %> #new_comment_form").before(comment);

comment.slideDown();

$("#<%= dom_id(@comment.photo) %> #new_comment_form #comment_body").val("");
